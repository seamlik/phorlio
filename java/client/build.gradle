apply plugin: 'application'

applicationName = rootProject.name
mainClassName = "${rootProject.name}.Main"

dependencies {
  implementation 'com.beust:jcommander:1.71'
  implementation project(':core')
}